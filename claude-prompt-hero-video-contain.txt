Goal: Switch hero background video from cover (cropped) to contain (letterbox) so the
full video content is visible without cropping. Letterbox areas show the dark background.

Only one small change in libs/components/layout/LayoutHome.tsx.

STRICT RULES:
- Do NOT touch anything other than the video element's style object.
- Do NOT change WORDS, canvas, animation, buttons, overlay, or SCSS.

=====================================================================
FILE: libs/components/layout/LayoutHome.tsx
=====================================================================

--- STEP 1: Change objectFit from cover to contain, add dark background for letterbox areas ---
Find:
								style={{
									position: 'absolute',
									inset: 0,
									width: '100%',
									height: '100%',
									objectFit: 'cover',
									zIndex: 0,
									pointerEvents: 'none',
								}}

Replace with:
								style={{
									position: 'absolute',
									inset: 0,
									width: '100%',
									height: '100%',
									objectFit: 'contain',
									backgroundColor: '#0a0a14',
									zIndex: 0,
									pointerEvents: 'none',
								}}

=====================================================================
Verify after implementing:
1. npx tsc --noEmit → 0 errors
2. Hero video is fully visible — no cropping, full frame shown
3. Letterbox areas (top/bottom or left/right) are solid dark (#0a0a14)
4. Word animation, buttons, subtitle — unchanged
