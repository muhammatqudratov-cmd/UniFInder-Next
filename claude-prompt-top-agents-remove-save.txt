Context: remove ALL save/bookmark related code from TopAgents.tsx.
Delete: BookmarkIcon, BookmarkBorderIcon imports, useMutation, useReactiveVar,
LIKE_TARGET_MEMBER, userVar, sweetTopSmallSuccessAlert, sweetMixinErrorAlert, Message imports,
savedAgents state, user reactive var, likeTargetMember mutation hook, and the entire bookmark button.
Keep everything else unchanged.

=====================================================================
FILE — libs/components/homepage/TopAgents.tsx
=====================================================================

CHANGE A — remove BookmarkBorderIcon and BookmarkIcon imports:

Find:
```tsx
import BookmarkBorderIcon from '@mui/icons-material/BookmarkBorder';
import BookmarkIcon from '@mui/icons-material/Bookmark';
import useDeviceDetect from '../../hooks/useDeviceDetect';
```

Replace with:
```tsx
import useDeviceDetect from '../../hooks/useDeviceDetect';
```

---

CHANGE B — revert Apollo import to useQuery only:

Find:
```tsx
import { useQuery, useMutation, useReactiveVar } from '@apollo/client';
```

Replace with:
```tsx
import { useQuery } from '@apollo/client';
```

---

CHANGE C — remove mutation, store, sweetAlert, and Message imports:

Find:
```tsx
import { LIKE_TARGET_MEMBER } from '../../../apollo/user/mutation';
import { userVar } from '../../../apollo/store';
import { sweetTopSmallSuccessAlert, sweetMixinErrorAlert } from '../../sweetAlert';
import { Message } from '../../enums/common.enum';
```

Replace with:
```tsx
```

(Delete all 4 lines — no replacement)

---

CHANGE D — remove savedAgents state, user reactive var, and likeTargetMember mutation hook:

Find:
```tsx
	const [topAgents, setTopAgents] = useState<Member[]>([]);
	const [savedAgents, setSavedAgents] = useState<Set<string>>(new Set());
	const user = useReactiveVar(userVar);

	/** APOLLO REQUESTS **/
	const [likeTargetMember] = useMutation(LIKE_TARGET_MEMBER);
	const {
```

Replace with:
```tsx
	const [topAgents, setTopAgents] = useState<Member[]>([]);

	/** APOLLO REQUESTS **/
	const {
```

---

CHANGE E — remove the bookmark button from the bottom action row, keep only "Get in touch":

Find:
```tsx
							{/* Bottom action row */}
							<div style={{ display: 'flex', gap: 10, alignItems: 'center', marginTop: 4 }}>
								<button
									onClick={(e) => { e.stopPropagation(); router.push({ pathname: '/agent/detail', query: { agentId: agent._id } }); }}
									style={{ flex: 1, height: 46, borderRadius: '50px', background: 'rgba(255,255,255,0.8)', border: '1px solid rgba(0,0,0,0.1)', fontSize: 14, fontWeight: 600, color: '#111', cursor: 'pointer', backdropFilter: 'blur(8px)' }}
								>
									Get in touch
								</button>
								<button
									onClick={async (e) => {
										e.stopPropagation();
										try {
											if (!user._id) throw new Error(Message.NOT_AUTHENTICATED);
											await likeTargetMember({ variables: { input: agent._id } });
											setSavedAgents((prev) => {
												const next = new Set(prev);
												next.add(agent._id);
												return next;
											});
											await sweetTopSmallSuccessAlert('Agent Saved', 1500);
										} catch (err: any) {
											sweetMixinErrorAlert(err.message).then();
										}
									}}
									style={{ width: 46, height: 46, borderRadius: '50%', background: '#fff', border: '1px solid rgba(0,0,0,0.1)', display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'pointer', flexShrink: 0 }}
								>
									{savedAgents.has(agent._id)
										? <BookmarkIcon style={{ fontSize: 20, color: '#111' }} />
										: <BookmarkBorderIcon style={{ fontSize: 20, color: '#555' }} />
									}
								</button>
							</div>
```

Replace with:
```tsx
							{/* Bottom action row */}
							<div style={{ display: 'flex', marginTop: 4 }}>
								<button
									onClick={(e) => { e.stopPropagation(); router.push({ pathname: '/agent/detail', query: { agentId: agent._id } }); }}
									style={{ flex: 1, height: 46, borderRadius: '50px', background: 'rgba(255,255,255,0.8)', border: '1px solid rgba(0,0,0,0.1)', fontSize: 14, fontWeight: 600, color: '#111', cursor: 'pointer', backdropFilter: 'blur(8px)' }}
								>
									Get in touch
								</button>
							</div>
```

---

Verify after implementing:
1. `npx tsc --noEmit` → 0 errors. No references to savedAgents, user, likeTargetMember, BookmarkIcon, BookmarkBorderIcon, Message, sweetTopSmallSuccessAlert, sweetMixinErrorAlert remain.
2. Homepage Top Agents: each card shows only the "Get in touch" button at the bottom — no bookmark icon.
3. Clicking "Get in touch" navigates to /agent/detail?agentId=... as before.
4. Hover effect (blur other cards on hover) still works — CSS unchanged.
5. Exactly 4 cards render (slice(0,4) unchanged).
