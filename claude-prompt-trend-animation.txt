Goal: Add scroll-triggered Slide + Blur entrance animation to Trend Universities cards.
When the .trend-cards-grid enters the viewport, each card animates in with:
  - opacity: 0 → 1
  - transform: translateY(24px) → translateY(0)
  - filter: blur(6px) → blur(0px)
  - easing: cubic-bezier(.16, 1, .3, 1)  duration: 0.6s
  - stagger: 130ms per card (card 0 = 0ms, card 1 = 130ms, card 2 = 260ms …)
  - plays ONCE per page load (observer disconnects after trigger)

STRICT RULES:
- Only edit: libs/components/homepage/TrendUniversities.tsx
- Do NOT change: Apollo queries, likeUniversityHandler, router.push, any props, defaultProps
- Do NOT add external animation libraries (no framer-motion, no gsap)
- Animation runs only on desktop (device !== 'mobile') render path

=====================================================================
FILE: libs/components/homepage/TrendUniversities.tsx
=====================================================================

--- STEP 1: Update import line ---

Find:
import React, { useState } from 'react';

Replace with:
import React, { useState, useRef, useEffect } from 'react';


--- STEP 2: Add gridRef and useEffect inside the component body ---

Find the line (inside the TrendUniversities component, right after the state declarations):
	/** APOLLO REQUESTS **/

Replace with:
	const gridRef = useRef<HTMLDivElement>(null);

	useEffect(() => {
		const grid = gridRef.current;
		if (!grid) return;

		const cards = Array.from(grid.querySelectorAll<HTMLElement>('.trend-uni-card'));
		if (cards.length === 0) return;

		// Set initial hidden state
		cards.forEach((card) => {
			card.style.opacity = '0';
			card.style.transform = 'translateY(24px)';
			card.style.filter = 'blur(6px)';
			card.style.transition = 'none';
		});

		const observer = new IntersectionObserver(
			(entries) => {
				if (entries[0].isIntersecting) {
					cards.forEach((card, i) => {
						setTimeout(() => {
							card.style.transition =
								'opacity 0.6s cubic-bezier(.16,1,.3,1), transform 0.6s cubic-bezier(.16,1,.3,1), filter 0.6s cubic-bezier(.16,1,.3,1)';
							card.style.opacity = '1';
							card.style.transform = 'translateY(0)';
							card.style.filter = 'blur(0px)';
						}, i * 130);
					});
					observer.disconnect();
				}
			},
			{ threshold: 0.08 },
		);

		observer.observe(grid);
		return () => observer.disconnect();
	}, [trendUniversities]);

	/** APOLLO REQUESTS **/


--- STEP 3: Add ref to the grid div ---

Find (in the desktop render path, inside the else block):
				<div className="trend-cards-grid">

Replace with:
				<div className="trend-cards-grid" ref={gridRef}>


=====================================================================
Verify after implementing:
1. npx tsc --noEmit → 0 errors
2. Open homepage, scroll down to Trend Universities section
3. Cards should start invisible (opacity 0, blurred, shifted down)
4. As the section enters the viewport, cards animate in one by one with 130ms stagger
5. Animation plays once — scrolling up and back down does NOT replay (observer disconnected)
6. Like button still works, router.push still navigates to detail page
7. Mobile render path is unchanged (no gridRef or useEffect affect the mobile Stack)
=====================================================================
