Context: bug fix + size increase for the Community page hero (`/community`), building on `claude-prompt-community-hero-highlight-and-triple-card.txt`. Two problems visible in the current render:
1. The "campus" word inside `.accent-chip` is rendering almost invisible (very light/white text on the pale coral chip background, and noticeably smaller than "Voices on") — it must be dark ink, same font-size/weight as "Voices on", just sitting inside the coral chip background.
2. The overall heading and the 3 right-side rotating cards should both be a bit bigger.
This is a CSS-only fix, no markup/logic change.

## 1. `scss/pc/main.scss` — `.community-heading` (bigger, and fix `.accent-chip` color/size)
Find the current `.community-heading` rule (inside `.header-basic.community-hero .community-hero-container`) and replace it with:
```scss
.community-heading {
	color: #1c1b18;
	font-family: $font;
	font-size: 64px;
	font-weight: 800;
	line-height: 1.15;
	letter-spacing: -1px;
	white-space: normal;

	.accent-chip {
		color: #1c1b18 !important;
		font-size: inherit;
		font-weight: inherit;
		font-family: inherit;
		background: #fbede7;
		padding: 2px 18px;
		border-radius: 14px;
		display: inline-block;
		line-height: 1.15;
	}
}
```
(The key fix is the explicit `color: #1c1b18 !important;` plus `font-size: inherit; font-weight: inherit;` on `.accent-chip` — without these it was inheriting a lighter/white color and shrinking instead of matching "Voices on".)

## 2. `scss/pc/main.scss` — `.browsing-card-stack .browsing-card` (bigger cards)
Find the current `.browsing-card-stack { .browsing-card { ... } }` rule and increase its sizing:
```scss
.browsing-card-stack {
	display: flex;
	flex-direction: column;
	gap: 12px;

	.browsing-card {
		flex-direction: row;
		align-items: center;
		gap: 12px;
		padding: 10px 10px 10px 22px;
		border-radius: 999px;
		background: #ffffff;
		border: 1px solid #e9e7e0;
		box-shadow: 0 1px 2px rgba(28, 27, 24, 0.03);
		min-width: 270px;

		.browsing-text {
			display: flex;
			flex-direction: column;
			gap: 3px;

			.browsing-label {
				color: #8e8c83;
				font-family: $font;
				font-size: 11px;
				font-weight: 700;
				letter-spacing: 0.8px;
				text-transform: uppercase;
			}
			.browsing-name {
				color: #1c1b18;
				font-family: $font;
				font-size: 15px;
				font-weight: 700;
				animation: browsingFadeIn 0.35s ease;
			}
		}
		.browsing-badge {
			width: 38px;
			height: 38px;
			border-radius: 50%;
			background: #1c1b18;
			color: #ffffff;
			font-family: $font;
			font-size: 11.5px;
			font-weight: 700;
			display: flex;
			align-items: center;
			justify-content: center;
			flex-shrink: 0;
			animation: browsingFadeIn 0.35s ease;
		}
	}
}
```
(Only sizes/padding increased — `min-width` 240→270, `border-radius`/font-sizes/badge size slightly up; structure, rotation logic, and colors stay the same as already implemented.)

Verify after implementing: "campus" now renders in the same solid ink color and size/weight as "Voices on" (just sitting inside the coral chip background, clearly readable), the whole heading is noticeably bigger, and the 3 stacked browsing cards on the right are visibly larger while still rotating correctly every 2 seconds with no duplicate university shown at the same time; no other page's hero or any logic is affected.
