Context: redesign the Agents list page to match the approved "Claude Design" visual language. This is a visual-only change: do NOT touch any GraphQL query/mutation, state, sorting logic, pagination logic, like-handler, or search-filter logic in `pages/agent/index.tsx` or `libs/components/common/AgentCard.tsx`. Only edit class-level CSS in `scss/pc/agent/agent.scss` (for `.agent-list-page`) and `scss/pc/general.scss` (for the `.agent-general-card` rule, which is what `AgentCard.tsx` renders). Keep the dark hero banner above this page exactly as it is (it's shared across other listing pages) — this prompt only covers the content area below the hero: the search/sort toolbar, the agent card grid, and the pagination/footer row.

Design tokens (exact hex values):
```
ink (headings/names):   #1C1B18
ink-2 (body text):      #57564F
ink-3 (muted/meta):     #8E8C83
line (borders):         #E9E7E0
line-2 (dividers):      #EFEDE7
surface:                 #FFFFFF
surface-2 (page bg):     #FAFAF8
coral (accent):          #E8856A
coral-deep (hover):      #D2674C
coral-tint (badge bg):   #FBEDE7
shadow-sm: 0 1px 2px rgba(28,27,24,.03)
shadow-md: 0 2px 8px rgba(28,27,24,.04)
shadow-lg: 0 8px 24px rgba(28,27,24,.05)
radius: pill 999px, cards 22px, inputs 14px
font: keep existing `$font` (Poppins) — do not add new font families
```

## 1. Page background (`#pc-wrap .agent-list-page`)
Change the content area background (below the hero) to `#FAFAF8` instead of the current beige/tan tone.

## 2. Search + sort toolbar (`.filter`)
- `.left input` (search field): `width:360px; height:50px; padding:0 20px; border-radius:999px; border:1px solid #E9E7E0; background:#FFFFFF; color:#1C1B18; font-size:14px;` placeholder color `#8E8C83`. On `:focus` → `border-color:#E8856A; box-shadow:0 0 0 3px rgba(232,133,106,.12);` (keep `outline:none`).
- `.right span` ("Sort by" label): `color:#8E8C83; font-size:13px; font-weight:500; margin-right:8px;`.
- `.right button` (the sort dropdown trigger showing the current sort name + chevron): restyle as a pill: `padding:9px 16px; border-radius:999px; border:1px solid #E9E7E0; background:#FFFFFF; color:#1C1B18; font-size:13px; font-weight:700;` hover → `border-color:#1C1B18;`. Keep the existing `Menu`/`MenuItem` (Recent/Oldest/Likes/Views) functionality exactly as-is — only restyle the `Menu` paper: `border-radius:14px; border:1px solid #E9E7E0; box-shadow:0 8px 24px rgba(28,27,24,.05); margin-top:8px;` and each `MenuItem`: `font-size:13.5px; color:#1C1B18; padding:10px 16px;` hover background `#F4F3EF`.

## 3. Agent card grid (`.card-wrap`)
- `display:flex; flex-wrap:wrap; gap:24px;` (replace any fixed right-margin spacing on individual cards with this gap so the grid is even).

## 4. Agent card (`.agent-general-card`, rendered by `AgentCard.tsx`)
- Remove the old plain/no-shadow look. Card wrapper: `width:266px; border-radius:22px; overflow:hidden; background:#FFFFFF; border:1px solid #E9E7E0; box-shadow:0 1px 2px rgba(28,27,24,.03); cursor:pointer; transition:transform .22s ease, box-shadow .22s ease, border-color .22s ease;` hover → `transform:translateY(-4px); box-shadow:0 8px 24px rgba(28,27,24,.05); border-color:#EFEDE7;`.
- `.agent-img`: `width:100%; height:233px; position:relative;` (keep the existing inline `backgroundImage` style untouched — only the container's size/radius). The "{count} universities" badge inside it (the `<div>` overlay): reposition to `top:14px; left:14px;` and restyle as a small pill/tag: `display:inline-block; padding:5px 12px; border-radius:999px; background:#FBEDE7; color:#D2674C; font-size:11px; font-weight:700; text-transform:uppercase; letter-spacing:.02em;` (replace the old solid `#eb6753` background).
- `.agent-desc`: `padding:16px 18px 18px; display:flex; flex-direction:row; justify-content:space-between; align-items:center;`.
  - `.agent-info strong` (name): `color:#1C1B18; font-size:15px; font-weight:700;`.
  - `.agent-info span` ("Agent" role label): `color:#8E8C83; font-size:12.5px; font-weight:500; margin-top:3px; display:block;`.
  - `.buttons` (views/likes icons row): icon color `#8E8C83`, hover/active like icon color `#E8856A`; `.view-cnt` text: `color:#8E8C83; font-size:13px; font-weight:500;`.

## 5. Pagination + footer row (`.pagination`)
- Style the MUI `Pagination` items to match the rest of the site: each page item `border-radius:8px; border:1px solid #E9E7E0; color:#1C1B18; font-weight:600;`, and `.Mui-selected` → `background-color:#E8856A !important; color:#FFFFFF !important; border-color:#E8856A;`.
- `.pagination span` ("Total {n} agents available"): `color:#8E8C83; font-size:13.5px; font-weight:500; margin-top:24px;`.
- `.no-data` (empty state, shown when `agents.length === 0`): center it, `gap:14px;`, the message text `color:#8E8C83; font-size:14px; font-weight:500;`, and the alert icon `opacity:.6;`.

Verify after implementing: searching by typing + Enter still filters, the sort menu (Recent/Oldest/Likes/Views) still re-sorts the list, pagination still navigates and updates the URL, liking an agent still calls the existing mutation and updates the like icon/count, and clicking a card still navigates to `/agent/detail` — only colors, spacing, radii, and shadows changed.
