Context: pure CSS change, no logic. This is the one remaining gap between the university card and the reference design: every card photo needs a dark gradient tint over it (the same black → forest-green → navy family used across the site's hero sections), so the photo reads as dark/moody behind the price tag and favorite button, matching the reference exactly. Everything else on the card (price top-left, favorite heart top-right, bottom row with eye+view-count and heart+like-count) is already correct and must not change.

This file's exact current content was just confirmed by reading the live file — match these find blocks exactly.

## `scss/pc/general.scss`

Find this exact block:

```scss
	.card-img {
		position: relative;
		width: 100%;
		height: 264px;
		background-size: cover;
		cursor: pointer;
		div.status {
			position: absolute;
			padding: 6px 12px;
			left: 12px;
			top: 12px;
```

Replace with:

```scss
	.card-img {
		position: relative;
		width: 100%;
		height: 264px;
		background-size: cover;
		cursor: pointer;
		&::before {
			content: '';
			position: absolute;
			inset: 0;
			z-index: 1;
			pointer-events: none;
			background: linear-gradient(
				135deg,
				rgba(21, 20, 15, 0.55) 0%,
				rgba(28, 38, 32, 0.45) 50%,
				rgba(26, 34, 48, 0.5) 100%
			);
		}
		div.status {
			position: absolute;
			z-index: 2;
			padding: 6px 12px;
			left: 12px;
			top: 12px;
```

Find this exact block:

```scss
		div.price {
			position: absolute;
			padding: 8px 14px;
			left: 12px;
			top: 12px;
			border-radius: 999px;
			background: rgba(255, 255, 255, 0.92);
			color: #1c1b18;
			font-family: $font;
			font-size: 14px;
			font-style: normal;
			font-weight: 700;
			line-height: normal;
			box-shadow: 0 1px 2px rgba(28, 27, 24, 0.03);
		}
```

Replace with:

```scss
		div.price {
			position: absolute;
			z-index: 2;
			padding: 8px 14px;
			left: 12px;
			top: 12px;
			border-radius: 999px;
			background: rgba(255, 255, 255, 0.92);
			color: #1c1b18;
			font-family: $font;
			font-size: 14px;
			font-style: normal;
			font-weight: 700;
			line-height: normal;
			box-shadow: 0 1px 2px rgba(28, 27, 24, 0.03);
		}
```

Find this exact block:

```scss
		.fav-btn {
			position: absolute;
			right: 12px;
			top: 12px;
			width: 34px;
```

Replace with:

```scss
		.fav-btn {
			position: absolute;
			z-index: 2;
			right: 12px;
			top: 12px;
			width: 34px;
```

Do not change anything else in this file — `div.status + div.price`, `.fav-btn`'s remaining properties, `.info`, `.bott`, `.buttons-box`, etc. are all already correct and must stay exactly as they are.

---

Verify after implementing:
1. Open `/agent/detail` (or any page rendering `UniversityBigCard`) — every card photo now shows a dark gradient tint over it, matching the reference design's moody dark look, regardless of the actual photo's real brightness/colors.
2. The price tag (top-left), "top" status badge (when present), and favorite heart button (top-right) are all still fully visible and clickable on top of the new tint — none of them are hidden behind it.
3. The bottom row still shows both eye+view-count and heart+like-count exactly as before — this prompt does not touch that area at all.
4. Clicking anywhere on the card still navigates to the university detail page, and clicking the favorite heart (top-right or bottom row) still toggles like state without navigating — the overlay has `pointer-events: none` so clicks pass through it untouched.
5. No GraphQL data, image URL, or event handler changed — only a decorative CSS layer plus three `z-index` additions to keep existing elements above it.
