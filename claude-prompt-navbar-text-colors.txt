Goal: Two changes to the navbar text behavior:
1. "UniFinder" logo text → white by default (matches nav links on dark hero)
2. When scrolled down (navbar-main gets class "transparent" = light pill on cream bg):
   ALL text turns dark: logo, nav links, active link — everything readable on light bg.

The glass pill shape itself stays unchanged (border, backdrop-filter, shadows).

2 files must be edited.

STRICT RULES:
- Do NOT touch: button logic, router.push, language switcher, canvas, hero, SCSS outside navbar.
- Do NOT change transparent class pill shape/background — only text colors inside it.

=====================================================================
FILE 1: libs/components/Top.tsx
=====================================================================

--- STEP 1: Add className="logo-circle" to the outer logo circle div,
    remove inline border color (border will be controlled by SCSS) ---

Find:
						<div style={{
									width: '28px',
									height: '28px',
									borderRadius: '50%',
									border: '2px solid #1C1B18',
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									flexShrink: 0,
								}}>

Replace with:
						<div className="logo-circle" style={{
									width: '28px',
									height: '28px',
									borderRadius: '50%',
									border: '2px solid rgba(255, 255, 255, 0.55)',
									display: 'flex',
									alignItems: 'center',
									justifyContent: 'center',
									flexShrink: 0,
								}}>


--- STEP 2: Add className="logo-text" to UniFinder span, remove inline color ---

Find:
						<span style={{ fontSize: '17px', fontWeight: '800', color: '#1C1B18' }}>UniFinder</span>

Replace with:
						<span className="logo-text" style={{ fontSize: '17px', fontWeight: '800', color: '#ffffff' }}>UniFinder</span>


=====================================================================
FILE 2: scss/pc/main.scss
=====================================================================

--- STEP 1: Add dark-text overrides inside the &.transparent block ---
    Find the transparent block and ADD the new rules at the end, before the closing brace.

Find:
			&.transparent {
				margin: 10px auto;
				display: flex;
				align-items: center;
				width: 90%;
				max-width: 1200px;
				border-radius: 999px;
				background: rgba(250, 250, 248, 0.97);
				backdrop-filter: blur(12px);
				-webkit-backdrop-filter: blur(12px);
				border: 1px solid #e9e7e0;
				box-shadow: 0 4px 16px rgba(0, 0, 0, 0.06);
			}

Replace with:
			&.transparent {
				margin: 10px auto;
				display: flex;
				align-items: center;
				width: 90%;
				max-width: 1200px;
				border-radius: 999px;
				background: rgba(250, 250, 248, 0.97);
				backdrop-filter: blur(12px);
				-webkit-backdrop-filter: blur(12px);
				border: 1px solid #e9e7e0;
				box-shadow: 0 4px 16px rgba(0, 0, 0, 0.06);

				/* === TEXT COLORS ON LIGHT BACKGROUND (scrolled) === */
				.logo-circle {
					border-color: #1c1b18 !important;
				}
				.logo-text {
					color: #1c1b18 !important;
				}
				.router-box .nav-item {
					color: rgba(28, 27, 24, 0.70) !important;
					text-shadow: none !important;
					&:hover {
						color: rgba(28, 27, 24, 0.95) !important;
						background: rgba(0, 0, 0, 0.05);
					}
				}
				.router-box a[aria-current='page'] .nav-item,
				.router-box a.active .nav-item {
					color: #1c1b18 !important;
					text-shadow: none !important;
				}
			}


=====================================================================
Verify after implementing:
1. npx tsc --noEmit → 0 errors
2. Home page (not scrolled, dark hero): "UniFinder" is WHITE, nav links WHITE
3. Scroll down (cream/white sections): "UniFinder" turns DARK (#1c1b18), nav links DARK
4. Glass pill border/shape/blur — unchanged in both states
5. Logo orange dot (8px circle) — unchanged
6. Language switcher, login button — unchanged
7. Mobile navbar — unchanged
